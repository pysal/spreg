name: test
channels:
  - conda-forge
dependencies:
  - python=3.9
  # required
  - numpy>=1.3
  - pandas>=1.0
  - scipy>=0.11
  # testing/formatting
  - codecov
  - pytest
  - pytest-cov
  # required
  - pip
  - pip:
    - git+https://github.com/pysal/libpysal.git@master
