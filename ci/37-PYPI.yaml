name: test
channels:
  - conda-forge
dependencies:
  - python=3.7
  # required
  - libpysal
  - numpy>=1.3
  - pandas>=1.0
  - scipy>=0.11
  # testing/formatting
  - codecov
  - pytest
  - pytest-cov
